# Psych101 Textbook — Handoff Document

> **How to use this file:** The Status and Next Up sections are overwritten each session. The Session Log at the bottom is append-only — newest entry at the top. **Archival trigger:** once the Session Log holds more than 10 entries, move the oldest entries down to 8 remaining into `HANDOFF-ARCHIVE.md` verbatim (append under a new dated/labeled section, don't overwrite prior archive content). Entries should run ~150–250 words each per the project's own logging instructions — at that size 10 entries is ~20–30KB, nowhere near a size problem on its own; the trigger exists to catch entries that overrun that word target (this has happened before: several past entries ran 750–1500+ words). This is separate from Current Status, which must be *overwritten* every session, never accumulated — its own bloat (144KB by Session 79) came from that rule being skipped, not from Session Log length.

Detailed chronological GPT/Codex work history now lives in `GPT_project_log.md`. Repository-level agent instructions live in `AGENTS.md`. `HANDOFF.md` should stay concise and current-state oriented.

> **Start-of-session briefing — do this first in every new chat, before any work:**
> 1. **Confirm the repo path.** This file is canonical only at `C:\GitHub\psych101`. A renamed stale copy exists at `C:\Users\oxfor\OneDrive\Docs\GitHub\old_psych101` — if that, or any similarly-named folder, is the one connected, it is out of date; ignore it and confirm with Jon before trusting anything read from it.
> 2. **Run the context budget meter** — `python pipeline/context_budget.py --log` — before making any edits this session. It appends one dated row (tokens per category: HANDOFF, reference CSVs, specs, chapter markdown, provenance, built HTML) to `pipeline/context_budget_log.csv`, so bloat is visible as a trend rather than rediscovered from scratch. **Treat its numbers as directional, not exact** — see the sandbox-mount caution below; a fresh session does *not* guarantee accurate reads.
> 3. **Brief Jon.** After reading Current Status + Next Up below, give a short plain-language summary — 2–4 sentences on where things stand, plus the top 2–3 open items from Next Up — then ask what he wants to focus on before starting.
> 4. **Check Session Log length.** Count the `### Session` headings below. If more than 10, archive the oldest down to 8 remaining (see "Archival trigger" above) before or after this session's own work — whichever is more convenient, but don't skip it.
>
> **Standing caution — sandbox-mount staleness (root cause confirmed Session 90):** bash's view of files in this repo can lag the true file state by hours, and — disproven this session — a fresh chat does *not* reset it. It only affects files last changed via the Edit/Write tools or externally (Jon's own edits, GitHub Desktop commits); a file bash *itself* just wrote via a heredoc is read back correctly. Matches two open public reports of the same bug: [anthropics/claude-code#55877](https://github.com/anthropics/claude-code/issues/55877) (rclone FUSE cache on the writable mount set to a 3600s/1hr duration) and [#38993](https://github.com/anthropics/claude-code/issues/38993) (virtiofs mount serving truncated/stale content, unaffected by remounting). Practical rules until Anthropic fixes it upstream:
> - **Never trust a bash read, `git status`/`git diff`, or a bash-run script's output as proof of a file's true content** — the Read tool (or `git show HEAD:<path>`, tested reliable) is ground truth.
> - **Before running any bash-based check (the linter, a python script, git) on a file edited via Edit/Write this session**, re-materialize that specific file through a bash heredoc (paste its true, Read-confirmed content into `cat > file <<'EOF' ... EOF`) first — the only fix that's worked so far. Do this only for files actually touched, not the whole repo.
> - Commits already go through Jon via GitHub Desktop rather than sandbox git — keep doing that; it's already insulated from this bug.

---

## Current Status

**As of Session 100 (2026-07-12).** Chapter 4's Session 99 source-to-HTML restoration is now automatically verified. The repository linter passes `docs/chapters/04-sensation-perception.html` with **0 failures and 0 warnings**. Supplemental checks confirm that the file ends with `</html>`; `<body>` and `<main>` each have one opening and one closing tag; no ampersand occurs outside a valid named or numeric entity; Review Questions run exactly 1–12 with no gaps or duplicates; and the Key Terms definition list contains 33 alternating `<dt>/<dd>` pairs with no orphaned term.

Targeted parity checks also confirm identical Review Question numbers and stems, identical 33-term Key Terms labels, and identical Chapter 4 lab destinations in `source/chapters/ch04-sensation-perception.md` and `docs/chapters/04-sensation-perception.html`. No Chapter 4 repair was needed during verification. Both Chapter 4 files remain modified and uncommitted; commits continue to go through Jon via GitHub Desktop. Unrelated work already present in the working tree was not touched.

## Next Up

- **Commit and push the verified Ch4 work** (`source/chapters/ch04-sensation-perception.md`, `docs/chapters/04-sensation-perception.html`) — Key Terms narrowing completed, source→HTML parity restored, all four new labs wired in, and the chapter now passes lint with 0 failures/0 warnings. Nothing is committed yet.
- **Live-test the four new Ch4 labs** (Signal Detection, Weber's Law, Blind-Spot Filling-In, Context-and-Ambiguous-Perception) in a real browser — built and wired by the concurrent session, never click-tested.
- **Build the size-weight illusion lab** (or drop the placeholder) — still says "when available" in both files; no such lab exists in `docs/labs/ch04/`.
- **Live-test both Chapter 3 labs** in a real browser at desktop, portrait-mobile, and landscape-mobile widths: complete each lab, reload mid-session, verify focus/state restoration, restart behavior, labels, and cue/outcome trace rendering.
- **Ch3 full line-by-line editorial pass** — Sections 3–4 and the back matter still haven't had the same review Section 1/2 just got.
- **Remove the leftover `[FLAG for Jon]` editorial note from Ch07's RLHF paragraph** (`docs/chapters/07-learning.html`, in the "But the analogy breaks" paragraph) — an internal review comment that shipped into live chapter text; Jon should decide on the underlying Process S/Pavlov's-bell pairing question it raises, then the bracket itself needs deleting either way.
- **Resolve the "Chapter 6" cross-references in Ch07 and Ch08's Connections tables** — both point at a "Ch. 6 — Sleep" that doesn't exist as built HTML; sleep content currently lives in Ch05 (Consciousness). Likely just needs the links retargeted to Ch05, but worth confirming against the intended final chapter map first.
- **Commit and push the Ch5/7/8 anchor/heading repair** (`docs/chapters/05-consciousness.html`, `07-learning.html`, `08-memory.html`, `pipeline/lint_chapters.py`) once the two items above are resolved — linter-clean (0 fails, 0 warns on all three) but not yet reviewed line-by-line by Jon.
- **Review and commit Ch11's Connections table + Stop & Retrieve conversion** (`source/chapters/ch11-social-psychology.md`, `docs/chapters/11-social-psychology.html`) — content is linter-clean (0 fails, 0 warns) but the 6 Connections rows and 5 callout prompts are newly authored and haven't had Jon's editorial pass yet.
- **Verify the two mobile fixes on an actual phone, both orientations** (`docs/css/style.css`, `docs/js/sidebar.js`) — sidebar visibility/reopen and expanded-figure clipping. Logic-tested in a jsdom simulation only; no real device or browser confirmation yet.
- **Ch2 expandable figures (2.1, 2.2, 2.3, 2.7, 2.9):** expand/collapse mechanic works; the mobile-clipping bug found this session is fixed pending device verification (see above). Caption legibility on mobile still not specifically checked.
- **Optional Ch2 image-folder cleanup:** `docs/images/ch02/` still has pre-existing orphaned/duplicate files not part of this pass's explicit archive list (e.g. `fig2-5-sampling-vs-assignment.png/.svg`, `fig_evidence_ladder.png`, `fig_double_blind_placebo.png`, `fig_operational_definition_reliability_validity.png`, `fig_type_i_type_ii_error_matrix.png`) — separate decision, not yet authorized.
- **Ch10 review-q markup:** convert its 12 review questions to the `<details>/<summary>` collapsible pattern (currently non-collapsible, confirmed by linter) — also add Stop & Retrieve (currently 0, same gap Ch9 had).
- **Continue `pipeline/sonnet5-improvement-plan.md`** — Task 0 (linter) is done; next is the remaining Task 0.5 context-hygiene items, then Ch11 parity (re-scoped, see above) → retrieval/review density → glossary linking + print CSS (the `<mark>` treatment above needs a grayscale-print check once built) → global glossary/search → template extraction.
- **Commit and push Session 87's Prologue edit** (`docs/chapters/prologue.html`, new image `docs/images/prologue/fig_interactive_imagery_book_ocean.png`) and verify Figure P.5 renders/expands correctly on the live site — not yet checked in a browser.
- **Audit the Session 80 sidebar-swap script.** Confirmed casualties now include not just the six chapter files (07/08/09/10/12/13, fixed Session 82) but also `docs/index.html` (severely truncated, undetected for ~6 sessions) and 3 lab HTML files (trivially truncated, missing trailing `>`). Worth a full sweep of all 18 files that script touched (see Session 80 log entry) rather than assuming these were the last casualties.
- **Decide whether to roll the expandable-figure pattern out further** (e.g. Ch10 Lifespan, Ch12 Emotion) — only Ch9 and the prologue use it so far, and the breakout margin just got bigger, worth a fresh look at whether Ch9's figures now look too wide.
- **Delete/disconnect the stale OneDrive copy** (`C:\Users\oxfor\OneDrive\Docs\GitHub\psych101` / `old_psych101`) from Cowork's connected folders — still connected, still a false-start risk.
- **Verify the live GitHub Pages site** for the six previously-restored chapters (07, 08, 09, 10, 12, 13) plus the Ch9 figure-expand feature — still not checked against the deployed site.
- **Review `pipeline/chatgpt-instructions.md`'s new content** — restructured version never explicitly reviewed/approved by Jon.
- **Sidebar collapse discoverability (open decision for Jon).** Small unlabeled hamburger icon is the only way to reopen a collapsed sidebar.
- **Ch10 Lifespan figures:** 8 figures built but have real defects (label boxes overlapping plotted lines) — fix list at `docs/images/ch10/ch10_figure_revisions.md`. Nothing wired yet.
- **Ch6 Sleep → HTML:** no HTML built yet; needs a first build once Jon has reviewed the markdown.
- Most/all of Ch2–13 markdown drafts are content-complete but still awaiting Jon's full line-by-line editorial review.
- `source/visuals-inventory.md` is known to drift out of sync with per-chapter image state — cross-check per-chapter `docs/images/chXX/README*.md` files before trusting it.

## Session Log

### Session 100 (2026-07-12)

**What happened:** Picked up Session 99's outstanding Chapter 4 verification in the canonical `C:\GitHub\psych101` repository. Ran the mandatory context-budget meter, then invoked the repository's targeted chapter linter against `docs/chapters/04-sensation-perception.html` using the bundled Python runtime because this shell did not expose a `python` command directly. The result was a clean **PASS: 0 hard failures, 0 warnings**; unlike Session 99's stale mount, this run read the current 103,514-byte HTML file and produced no truncation or structural complaints.

Performed independent checks for the specific risks Jon named. The HTML ends with `</html>`; `<body>` and `<main>` each balance 1:1; every ampersand begins a valid named or numeric entity; Review Questions are numbered consecutively 1 through 12; and the Key Terms `<dl>` contains 33 `<dt>` elements alternating one-for-one with 33 `<dd>` elements. Also compared the two Chapter 4 representations directly: all 12 review-question numbers and stems match, all 33 Key Terms labels match, and the Chapter 4 lab link destinations match between source markdown and built HTML. No content or markup defect was found, so neither Chapter 4 file needed repair. Updated Current Status and Next Up to mark verification complete and leave commit/push as Jon's next action. Existing unrelated working-tree changes were left untouched. Nothing committed.

### Session 99 (2026-07-11)

**What happened:** Asked to "finish Chapter 4." Before touching anything, re-checked the four Ch4 Next Up items against current git state and found HANDOFF was stale: a concurrent/parallel session (commits "202"/"204"/"218", authored via Claude/Cowork, landing the same afternoon) had already committed a large independent rewrite of the source markdown — consolidated LOs, a new pictorial depth-cues table, reworded Hubel-Wiesel and dress paragraphs, both orphaned references now cited inline, harm-avoidance dropped from the Individual Differences table, and review questions already trimmed 13→12 with a new common-fate question filling a real coverage gap. That branch also built four new Ch4 labs and a lab hub page. A merge (`26c91ac`) had apparently kept Session 98's HANDOFF narrative text while the other branch's actual file changes landed underneath it, so HANDOFF never reflected any of this. Surfaced this to Jon directly rather than silently proceeding on stale assumptions or silently redoing already-completed work; he confirmed and asked to (a) port the source changes into HTML, (b) finish the Key Terms narrowing in the direction already started, and (c) wire the four new labs into the chapter text.

**Key Terms:** applied a consistent rule — add an entry only if the term is named in a Learning Objective or is what a review question is actually testing; otherwise unbold and leave unglossed. Added Hit/Miss/False alarm/Correct rejection (Review Q3 tests exactly these four) and Frequency theory (LO4 names it alongside Place theory, which already had an entry — this was a real asymmetry, not a scope call). Unbolded the rest of the audit's flagged cluster: retina, fovea, CNN, Tesla Vision, the six individually-named Gestalt principles (the collective "Gestalt principles" entry already lists all six), Müller-Lyer illusion, auditory canal, hair cells, somatotopic map, and the four other-senses terms. Table row-label bolding (the depth-cue tables) was treated as pure formatting, not a glossary-completeness signal — noting this explicitly since it's a judgment call future sessions should know about. Also caught and fixed a new orphaned reference: Cloninger (1987) was left in the References list after the other branch's edit removed the harm-avoidance sentence that had cited it.

**HTML parity:** ported every source-side change into `docs/chapters/04-sensation-perception.html` — LOs, both depth-cue tables (including the new 7-row pictorial one), the reworded Hubel-Wiesel/dress/Tesla-Vision paragraphs, the simplified Individual Differences row, all 12 review questions (renumbered correctly, plus a small option-wording fix on Q6 to match source), and the full narrowed/expanded Key Terms list.

**Lab wiring:** repointed the Weber's Law and Signal Detection links (previously pointing at the old combined-demos redirect stub) to their new dedicated pages, and added links to the two previously-unwired new labs — Blind-Spot Filling-In at the Misconception Opener's blind-spot paragraph, Context-and-Ambiguous-Perception after the dress-illusion material in Section 3. Applied identically to both files.

**Verification gap:** bash's linter run on the freshly-edited HTML reported truncation and a line count below what the Read tool confirmed — the documented sandbox-mount staleness bug. Re-materializing an 800+ line file via bash heredoc by hand was judged too error-prone; instead did three full sequential Read-tool passes over the entire HTML file (confirmed proper `<!DOCTYPE>` through `</html>` closure, no orphaned tags, all edits landed) and targeted Grep checks (no stale lab links remaining, no Cloninger/harm-avoidance remnants, clean 1–12 review-question sequence). This is real but weaker evidence than a clean linter run — flagged as the top Next Up item.

**Files modified:** `source/chapters/ch04-sensation-perception.md`, `docs/chapters/04-sensation-perception.html`, `HANDOFF.md`. Nothing committed.

### Session 98 (2026-07-11)

**What happened:** Jon asked to look at `pipeline/audits/ch04-pre-review-audit.md`, a same-day audit of Ch4 Sensation & Perception's source/HTML parity, then asked directly whether the flagged differences meant HTML had been updated and source left stale. Rather than answering from the audit alone, traced the full git history of both files: found it wasn't one-directional. Source got a large v0.4 content rewrite (`f8b0b44`, Jun 30) that HTML didn't get same-day; HTML then got an incomplete same-week catch-up pass (five commits, Jul 1) that didn't fully port everything back; both got an identical chapter-renumbering fix (Jul 3, near-simultaneous commits); and HTML got an unrelated, purely mechanical sidebar-swap (Jul 9) that isn't a content issue at all.

**Fixed the mechanical findings in both files:** a dead Heider-Simmel lab link in source (pointed at a nonexistent `docs/labs/motion/` path; confirmed via `ls` that only `docs/labs/ch04/heider-simmel/` exists, and corrected source to match HTML's already-working path), a fabricated Random Dot Kinematogram link in both files (confirmed no such lab exists anywhere in `docs/labs/`; rephrased the sentence to drop the false promise rather than invent a URL), four stale Connections-table title strings in both files (dropped "(review)" tags, matched the site's `sidebar.js` nav-label convention for Ch. 13, restored a dropped "Lifespan" in the Ch. 10 row), and a two-word omission in HTML's Chapter Summary Gestalt list.

**Resolved the audit's two "materially different" flags** by reading both files in full rather than trusting line-number pointers: Gestalt body prose was already near-identical (just that one Chapter Summary gap); Hubel-Wiesel's difference was purely presentational (source: plain paragraphs; HTML: a six-field Classic Study callout, identical facts). Walked Jon through a side-by-side of both. Reviewed the HTML-only "W/M good continuation" Think About It box and its original diagram — raised a legibility concern (does the reader actually see "W" and "M" before the reveal); Jon correctly pointed out the panel's own sub-caption already primes it, resolving the concern. Ported all three remaining gaps into source: the W/M box + image, Hubel-Wiesel restructured into the matching six-field format (reused this project's existing bold-label "Classic Study" convention from ch12's Patient SM section rather than inventing new markdown), and the Gestalt figure swap to the original 6-panel diagram — which also clears the audit's missing-reference flag, since the Spielman/OpenStax citation is now gone from source entirely.

**Also restructured the Session Log archive** while doing the standing session-count check: 11 real entries were present (more than the 10-entry trigger), so moved Sessions 88–90 verbatim into `HANDOFF-ARCHIVE.md` (new "Archived Session Log (Sessions 88–90)" section) and consolidated the two resulting archive pointers.

**Not done:** the two orphaned references (Enns & Lleras 2008, Tesla n.d.), the Key Terms gaps, and the 13→11-12 review-question trim — all logged to Next Up with specifics from the audit.

**Files modified:** `source/chapters/ch04-sensation-perception.md`, `docs/chapters/04-sensation-perception.html`, `HANDOFF.md`, `HANDOFF-ARCHIVE.md`. Nothing committed.

### Session 97 (2026-07-11)

**What happened:** Picked up the four items Session 96 deferred to a fresh chat, working through Ch3 top to bottom. First checked the top Next Up item (a new custom figure for the synapse/reuptake/SSRI paragraph) and found it already resolved — Jon had generated and committed Figure 3.6 himself that same morning (`git log` showed commit `122c485`, and the image README already documented it as wired) — Next Up just hadn't caught up. Surfaced this rather than re-asking Jon for a spec on a figure that already existed.

**Section 2 reorganization:** Drafted a three-block regroup (Crossing the Synapse / Changing the Next Cell / Changing the Whole System) and shared it for review before touching any files. Jon's critique identified a real flaw: folding neuromodulators and hormones into one "Whole System" block broke the section's own opening Do Not Confuse distinction (neural-projection route vs. bloodstream route) and risked reinforcing "neuromodulators are basically hormones in the brain." Adopted his four-block structure instead (added "Changing a Circuit" as its own block, hormones become "Changing the Whole Body"), including his bridge sentence, the oxytocin "complicates the categories" reframe, and a retrieval callback at the endocrine transition. Applied to both source and HTML; updated the one stale Connections-table anchor that pointed at the old header id.

**Opener/Section 1 relevance check:** Went paragraph by paragraph against the chapter question. Found one genuine cut — the 86-billion-neuron-count paragraph — after Jon applied a clean test ("I wouldn't ask exam questions about it"). Claude had initially defended the paragraph by claiming it anchored Review Q12; this was wrong (Q12 is anchored to the 20%-metabolic-energy figure from the opening myth box instead) and was corrected in the same turn rather than left standing. Cut the paragraph plus its two now-orphaned references (Herculano-Houzel 2009; Williams & Herrup 1988) from both files.

**Also applied:** the already-drafted Q12 replacement (serotonin/SSRI causal-inference question) and the cortisol/Section 3 timing fix (removed "not useful in the next sixty seconds," which could be misread as a claim about cortisol's own onset speed against Section 3's explicit fast/slow framing).

**Verification:** hit the sandbox-mount staleness bug again (bash undercounted both files — HTML missing its last ~26 lines including `</html>`; source .md off by 22 lines). Read tool and Grep (targeted at the Windows path, not the bash mount) both confirmed ground truth directly; no re-materialization needed. Checked tag/id balance on the HTML (paragraph and h3 open/close counts, all header ids unique) as a linter substitute since bash couldn't be trusted on this file this session.

**Files modified:** `docs/chapters/03-neuroscience.html`, `source/chapters/ch03-neuroscience-biological-bases.md`, `HANDOFF.md`. Nothing committed.

### Session 96 (2026-07-11)

**What happened:** Continued Ch3's accuracy/cognitive-load pass from an external editorial review, verifying every claim against the actual files first — confirmed the chapter is structurally complete (9 figures, Connections table, 12 review questions with hidden rationales, Key Terms, Further Reading, References) and confirmed `source/visuals-inventory.md` is genuinely stale (describes a superseded figure set). Worked through the review's Section 1 fixes collaboratively rather than applying them as-is. Jon corrected the Gage-survival rewrite twice — first supplying the anatomically precise reason (brainstem spared, cortical plasticity, PFC organizes-not-sustains), then simplifying further for gen-psych level (brainstem-spared only, plasticity language deferred to Section 4). Independently caught a sharper contradiction the original review missed: the old "no single neuron... does without" line directly contradicted the myth paragraph's own clinical-evidence claim two paragraphs earlier, not just Section 4's later material. Iterated the pins-and-needles box through several drafts after Jon flagged that "sodium-potassium pump" was never established as known vocabulary, and that two hedging sentences were too verbose.

**Applied to both source and HTML:** myth-paragraph trim (10 packed claims → 4; child-cost figure stays in Chapter Summary), Gage-survival sentence + new bridge sentence, pins-and-needles box rewrite.

**Verification:** hit the sandbox-mount staleness bug again on the edited HTML (bash's linter false-failed on truncation; `wc -l` showed 771 vs. Read-confirmed 792) — Read-tool inspection confirmed the file is genuinely well-formed.

**Not done:** paragraph-relevance check for the rest of Section 1, and Section 2's reorganization — deferred to a fresh chat. Also open: the cortisol/Section 3 timing tension and a ready Review Q12 replacement, both flagged but not applied.

**Files modified:** `docs/chapters/03-neuroscience.html`, `source/chapters/ch03-neuroscience-biological-bases.md`, `HANDOFF.md`. Nothing committed.

### Session 95 (2026-07-11)

**What happened:** Jon asked to work on Ch3 next, scoped down to an images/figures pass via `AskUserQuestion`. Read `docs/images/ch03/README_captions_alt_text_attribution.md` and grepped the actual source/HTML for image references before trusting the README's plan — found all 9 planned figures already wired correctly with matching alt text (linter clean), contrary to what the folder's "not used" subfolder naming implied. Surfaced three genuine gaps and asked Jon to decide each rather than assuming: (1) an unwired original stress-response diagram — Jon confirmed it was a placeholder, left out; (2) no figure for the synapse/reuptake/SSRI paragraph despite two retired candidates — Jon declined both, wants a new one generated to his spec (open item); (3) a still-deferred saltatory-conduction video with no attribution on record — asked Jon directly rather than guessing, confirmed Google AI-generated, and he said embed it now.

**Implemented:** Renamed `Saltatory conduction vid.mp4` → `ch03_saltatory_conduction_demo.mp4`; added a `<video>` figure ("Video 3.1") after Figure 3.4 in both the markdown source and HTML, matching the existing figure/figcaption convention; added a `.chapter-figure video` CSS rule so it renders like the image figures. Rewrote the Ch3 image README to fix a Session-50 off-by-one figure-numbering error (Figs 3.1–3.7 all mislabeled) and document all three decisions above.

**Verification:** Hit the sandbox-mount staleness bug on the file just edited — bash-run linter falsely reported `03-neuroscience.html` as truncated with unclosed `<body>`/`<main>`. Read the full 762-line file via the Read tool and confirmed it is genuinely complete and well-formed; did not chase a bash re-materialization since the Read-tool check was conclusive.

**Files modified:** `docs/chapters/03-neuroscience.html`, `source/chapters/ch03-neuroscience-biological-bases.md`, `docs/css/style.css`, `docs/images/ch03/README_captions_alt_text_attribution.md`, one file renamed in `docs/images/ch03/`, `HANDOFF.md`. Nothing committed.

### Session 94 (2026-07-11)

**What happened:** Completed the narrow render-output repair requested for Chapters 5, 7, and 8. In Ch5, corrected both sleep-section Connections links, gave the existing Circadian Rhythms and Sleep Pressure `<h3>` a precise stable ID, and retargeted its Connections row. In Ch7, converted all eight source-backed literal `####` paragraphs to `<h4>` headings with stable slugs and corrected the Section 1 double-dash target. In Ch8, converted all six source-backed literal `####` paragraphs to `<h4>` headings and corrected all seven double-dash Connections targets. No additional affected headings were found beyond the supplied lists; no source prose changed.

**Root cause and guard:** No checked-in chapter conversion script exists to repair narrowly, so no uncertain pipeline change was attempted. Added a focused hard-fail rule to `pipeline/lint_chapters.py` for paragraph text beginning with Markdown heading markers. Repository-wide search found no remaining literal paragraph headings. Every internal target in Ch5/7/8 exists exactly once; no duplicate IDs or consecutive-hyphen targets remain; repaired headings exactly match source wording/order and sit under the correct `<h2>` sections. Ch5/7/8 each lint at 0 failures, 0 warnings. Full lint remains 12/13 hard-clean because of Ch10's pre-existing non-collapsible review questions; unrelated existing warnings remain in Ch1, Ch4, Ch10, Ch12, and Ch13.

**Files modified:** `docs/chapters/05-consciousness.html`, `docs/chapters/07-learning.html`, `docs/chapters/08-memory.html`, `pipeline/lint_chapters.py`, `pipeline/context_budget_log.csv`, `HANDOFF.md`, and `GPT_project_log.md`. Nothing committed or pushed.

**Addendum, same session: Claude verification pass, plus two findings Codex's own report didn't surface.** Independently confirmed the repair was correct by reading the actual edited files via the Read tool rather than trusting bash, which showed a stale/truncated snapshot of all three chapter HTML files and of `lint_chapters.py` itself (still mid-edit in bash's cached view, missing its closing `main()` guard) — the documented sandbox-mount bug, caught live rather than inferred after the fact. Re-materialized `lint_chapters.py` from the Read-confirmed content to get a trustworthy linter run. Verified individually: all 8 Ch07 and 6 Ch08 literal-`####` paragraphs are now real `<h4 id="...">` elements matching source wording; every flagged double-dash Connections link in Ch5/7/8 resolves correctly; Ch05's previously id-less "Circadian Rhythms and Sleep Pressure" `<h3>` now has `id="circadian-rhythms-and-sleep-pressure"` and its link retargeted. This also matched and confirmed a broader defect count Jon separately got from a parallel GPT audit (8 and 6, not the 4 Claude had found on a first partial pass checking only linked headings) — cross-validation catching a real gap.

Two things not caught by Codex's own repair report: a leftover `[FLAG for Jon: ...]` editorial bracket note shipped live inside Ch07's RLHF paragraph, and both Ch07/Ch08 Connections tables citing "Chapter 6 — Sleep" for content that currently lives in Ch05 — both logged to Next Up, neither fixed this session.

Also built `pipeline/codex-task-template.md` at Jon's request — a reusable, fill-in-the-blank version of the prompt that produced this clean a repair, intended to make future narrow/mechanical Codex handoffs faster and keep Claude's own token usage focused on judgment calls rather than routine repair verification.

**Files modified (addendum):** `pipeline/codex-task-template.md` (new). `HANDOFF.md` (Current Status, Next Up, this entry). Nothing committed.

### Session 93 (2026-07-11)

**What happened:** Jon asked to tackle the HANDOFF.md cleanup item Session 92 had flagged and deferred. Investigated the truncated `### 2` line at the file's tail before archiving anything, per the standing "don't guess at what belongs there" instruction. Used `git log --format=%H --reverse -- HANDOFF.md` to walk every commit touching the file and diffed line counts across the sequence: found a real drop between `b1309839` (604 lines, Sessions 60–79b fully intact) and the immediately following commit `050eecbc` (343 lines, Session 62 cut off mid-sentence, Sessions 60–61 gone entirely) — no archive footer or commit message documents this as intentional, unlike the genuine Sessions-1–59 archive that preceded it. Concluded this was the same silent tooling-truncation bug this project has hit before (Sessions 81/82/86), just never caught at the HANDOFF.md level.

**Recovered and archived.** Pulled Sessions 60–64's full text verbatim from `b1309839` via `git show`. Combined with Sessions 65–84 (read directly from HANDOFF.md's current working copy via the Read tool, cross-checked against a fresh bash read of the same lines to confirm the sandbox mount wasn't stale before trusting it). Appended all 25 entries, newest-first, to `HANDOFF-ARCHIVE.md` under a new "Archived Session Log (Sessions 60–84)" section with an explicit recovery note explaining the truncation and the git commits involved. Verified the archive write via the Read tool (ground truth per this file's own standing caution) before trusting it. Trimmed HANDOFF.md's own Session Log down to Sessions 85–92 (now 85–93), replacing the removed entries with a one-line pointer to the archive.

**Not done:** did not investigate *why* the truncation happened (root cause of that specific commit, as opposed to the general sandbox-mount bug already documented) — flagged as low-value forensics on a bug this file already has a standing mitigation for. Did not touch any chapter content or commit anything.

**Files modified:** `HANDOFF.md` (Session Log trimmed, Current Status/Next Up rewritten, this entry), `HANDOFF-ARCHIVE.md` (new archived section, Sessions 60–84). Committed and pushed by Jon via GitHub Desktop later the same session (`baf11f4`) — see the addendum below.

**Addendum, same session: verified Jon's push.** After Jon confirmed he'd pushed, `git status` in the sandbox showed ~76 files as modified — alarming at first glance, but `git diff -w` (whitespace-insensitive) on a sample of them (`GPT_spine.md`, `_archive/ChatGPT_spine_revisions_proposal.md`, `source/visuals-inventory.md`) showed the diffs vanish completely, confirming pre-existing CRLF/LF line-ending drift on this repo rather than real uncommitted content — same pattern Session 80 already documented for this repo. Cross-checked `git show HEAD:HANDOFF.md` directly against the Read-tool-confirmed working copy to confirm the commit itself captured the full, correct file (not a truncated snapshot). Confirmed Session 91's Ch1/Ch2 content and this session's archive work are genuinely committed and pushed. Removed the now-completed "Commit via GitHub Desktop" item from Next Up.

**Files modified (addendum):** `HANDOFF.md` (Current Status updated, Next Up item removed). No other files touched.

**Second addendum, same session: two live mobile bugs, diagnosed and fixed.** Jon reported no chapter sidebar visible on his phone in portrait; followed up that it appears "turned sideways" (landscape works). Read `docs/css/style.css` and `docs/js/sidebar.js` directly: found the site's only responsive rule, `@media (max-width: 700px)`, hid both `.sidebar` and `.sidebar-toggle` outright with no fallback — and since `.sidebar-toggle--float` (the collapse/reopen hamburger already built and working on desktop per Session 80/80b) also carries the `.sidebar-toggle` class, that rule nuked the one thing that would've let a user get back in. Width-only also meant landscape phones (commonly 750–930px wide) skipped the rule entirely, reverting to full desktop layout — which is why it "worked" sideways. While fixing this, Jon separately reported a Ch2 expanded-figure image getting clipped in landscape (fine in portrait) with no way to scroll to the rest. Traced to the same root cause: the desktop expanded-figure breakout margin (`clamp(-220px, -14vw, 0)`) is computed against full viewport width, but in landscape the old fixed 260px sidebar column was still eating real space the math didn't know about, pushing the image past the visible screen edge.

**Fix, both bugs, one root cause.** Broadened the breakpoint to `@media (max-width: 700px), (hover: none) and (pointer: coarse)` — catches touch devices by input type, not just width, so landscape phones are covered too. Made `.sidebar` `position: fixed` (an overlay, out of the flex flow so it never competes with `.main` for width) instead of `display: none`, reusing the existing collapse/floating-toggle JS mechanic rather than building new UI. Added a mobile-only default: sidebar starts collapsed on first visit if there's no saved preference (`docs/js/sidebar.js`, `MOBILE_QUERY` constant kept in sync with the CSS query string by comment). Added a hard `width: 100vw` cap on `.chapter-figure.expandable.is-expanded` inside the same mobile block so expanded figures can never overflow the screen regardless of viewport width or sidebar state, replacing reliance on the desktop vw-clamp math.

**Verification:** installed `jsdom` and wrote a 5-scenario behavioral test for the sidebar JS logic (mobile/desktop × no-stored-pref/stored-collapsed/stored-explicitly-expanded) — all passed, confirming the new default-collapse logic doesn't regress existing desktop behavior or override a user's explicit prior choice. Hit the sandbox-mount staleness bug again mid-test (bash's cached copy of the freshly-edited `sidebar.js` was truncated at a comment mid-sentence); caught it by comparing against the Read-tool version rather than trusting the first test failure, re-materialized the file via heredoc from the Read-confirmed content, then re-ran the test clean. CSS brace-balance checked (90 open/90 close) and file tail confirmed unterminated-block-free via Read. **Not done:** no real device or browser verification — this is logic/syntax verification only, flagged explicitly in Next Up.

**Files modified:** `docs/css/style.css` (mobile media query broadened; sidebar overlay rules; expanded-figure mobile width cap), `docs/js/sidebar.js` (mobile-query constant; default-collapsed-on-first-mobile-visit logic), `HANDOFF.md` (Current Status, Next Up, this entry). Nothing committed — pending Jon's device test and a GitHub Desktop push.

**Third addendum, same session: Ch11 Task 1 (Connections table + Stop & Retrieve) finished.** Re-scoped per Session 92/93's own Next Up: checked the linter and grepped the actual chapter before starting rather than trusting the plan doc, and confirmed review-question markup, Further Reading, and References were already done in an earlier undocumented pass — only the Connections table and the Stop & Retrieve format decision were genuinely open.

Added a 6-row **Connections** table to `source/chapters/ch11-social-psychology.md` and `docs/chapters/11-social-psychology.html`, placed before Key Terms, matching the plain-text `<td>` convention every sibling chapter actually uses (checked Ch4/Ch7/Ch8/Ch9/Ch10/Ch12/Ch13's real HTML — none hyperlink cells despite the plan doc saying "hyperlinked"; established practice won over stale plan text). All 6 rows are grounded in cross-references already present in the chapter's own text — aggression neuroscience → Ch3, Rorschach reliability/validity → Ch2, Bandura's Bobo doll → Ch7, stereotype threat → Ch9, temperament → Ch10, social judgment setting up emotion/stress → Ch12 — none fabricated.

For the Stop & Retrieve format question, grepped every sibling chapter's *current* HTML rather than trusting HANDOFF's own prior note (which implied Ch9 still used a standalone section): the inline `callout--stop-retrieve` pattern is now the unanimous convention across every chapter that has one, including Ch9 (converted back in Session 89, HANDOFF's note was just stale). Converted Ch11 from its old standalone 8-item numbered-list "Stop & Retrieve" section to 5 inline callouts at natural section-pause points (the dissonance-routes paragraph, the SPE-limits paragraph, the groupthink paragraph, the bystander cost-benefit paragraph, the person-situation closing paragraph), in both source (blockquote format) and HTML (`callout--stop-retrieve` div), matching Ch8's clean reference implementation where source and HTML stay in sync — a property Ch9 and Ch12's markdown sources notably lack (HTML-only drift, not fixed here, just not perpetuated).

**Verification:** hit the sandbox-mount staleness bug validating the HTML edits — `lint_chapters.py` run via bash false-failed twice in a row (truncation, unbalanced tags, missing Further Reading) against a cached copy of the file truncated mid-word at two different line counts across a `sleep 3` retry. Read the full 693-line file end-to-end via the Read tool to confirm it was actually complete and correct, then re-materialized it into the sandbox via a heredoc built from that Read-confirmed content — the file's own standing-caution workaround — before re-running the linter. Result: 0 fails, 0 warns. Also updated `pipeline/sonnet5-improvement-plan.md`'s Task 1 section to mark it done, correct its stale "steps 1/3/4 not started" framing, and record what was actually found and done.

**Files modified:** `source/chapters/ch11-social-psychology.md` (Connections table, Stop & Retrieve inline callouts, standalone section removed), `docs/chapters/11-social-psychology.html` (same), `pipeline/sonnet5-improvement-plan.md` (Task 1 marked done), `HANDOFF.md` (Current Status, Next Up, this entry). Nothing committed — pending Jon's editorial review of the new Connections content and callout wording, per standing convention of committing via GitHub Desktop only.

### Session 92 (2026-07-10)

**What happened:** Jon reported a live bug: Ch2's newly-wired expandable figures (Session 91) expanded on click but never collapsed — only the browser back button got him out. Compared Ch2's HTML against Ch9 and the Prologue, both of which use the same `.chapter-figure.expandable` pattern and work correctly. Found the difference: Ch2 had all the right markup (`expandable`/`figure-expand-toggle` classes on all five figures — 2.1, 2.2, 2.3, 2.7, 2.9) and the shared CSS applies fine, but `docs/chapters/02-research-methods.html` was missing the `<script src="../js/figure-expand.js">` include entirely — Ch9 and the Prologue both have it, Ch2 never got it added during Session 91's image-wiring pass. Without the script, clicking the anchor did the browser's default thing (navigate to the raw image file) instead of the JS toggle, which is exactly the "expands, can't collapse, need back button" symptom. Fixed with a one-line addition before `</body>`. Jon confirmed via live testing that it now works. (One wrinkle mid-diagnosis: a bash grep for the script tag right after applying the fix came back empty, which looked alarming, but a direct `Read` confirmed the fix was actually in the file — the known sandbox-mount staleness bug, not a real problem.)

**Also surfaced, not acted on (Jon's call, deferred to next session):** the Session Log is at 30 entries (3x the 10-entry archive trigger, last archived Session 79b), and the file's last line is a truncated header (`### 2`) right after Session 65 — possibly real content loss matching this project's known truncation pattern, possibly something else; needs a git-history check before touching it.

**Files modified:** `docs/chapters/02-research-methods.html` (added missing `figure-expand.js` script include), `HANDOFF.md`. Nothing committed — folded into Session 91's still-pending GitHub Desktop commit.

### Session 91 (2026-07-10)

**What happened:** Three sequential blocks. (1) Finished Ch1: kept Section 3 title, documented Fig 1.1 attribution, cut Fig 1.2/Maslow (weak sourcing, archived), kept portrait strip, trimmed review questions 13→11. (2) Ran a full Ch2 line-by-line editorial pass converged against a parallel GPT audit — independently verified every factual claim (fetched the Wechsler et al. 2011 PubMed abstract directly rather than trusting reported numbers) before applying: reframed the evidence ladder as a seven-stage research cycle, added a convergence paragraph, broadened the control-group definition, corrected IRB/Belmont history (1974 National Research Act), added the Wechsler placebo-response-vs-effect prose, cut the CLT figure/demo entirely, settled the Noba citation-year convention (access date, not edition year), and trimmed review questions 13→11. (3) Executed a full Ch2 image-wiring pass against `CH02_IMAGE_WIRING_HANDOFF.md`: wired and renumbered nine figures (2.1–2.9), renamed a UUID-named third-variable asset after visually confirming its structure, repaired an incorrect embedded citation on the placebo PNG via targeted pixel edit (Python/PIL, bottom line only), archived four superseded assets, and updated the Ch2 image manifest and `visuals-inventory.md`.

**Files modified:** `source/chapters/ch01-history-approaches.md`, `docs/chapters/01-history-approaches.html`, `source/chapters/ch02-research-methods.md`, `docs/chapters/02-research-methods.html`, `docs/images/ch02/README_captions_alt_text_attribution.md`, `docs/images/ch02/ch02_placebo_response_vs_effect_asthma.png`, one file renamed in `docs/images/ch02/`, four files archived, `source/visuals-inventory.md`, `GPT_project_log.md`, `HANDOFF.md`. The sandbox-mount staleness bug recurred (bash-run linter false-failed against a stale HTML snapshot); worked around via direct `Read`-tool verification, not fixed. Nothing committed — flagged the handoff's commit-SHA request as conflicting with the GitHub-Desktop-only convention rather than running sandbox git.

### Sessions 85–90 — archived

Moved to `HANDOFF-ARCHIVE.md` on 2026-07-11 (Session 98; see "Archived Session Log (Sessions 88–90)" section there, plus the earlier "Archived Session Log (Sessions 85–87)" section) to bring this file back within the 10-entry archival trigger. Nothing was deleted — see `HANDOFF-ARCHIVE.md` for the full verbatim entries.

### Sessions 60–84 — archived

Moved to `HANDOFF-ARCHIVE.md` on 2026-07-11 (see "Archived Session Log (Sessions 60–84)" section there) to bring this file back within the 10-entry archival trigger. This includes Sessions 60–64, which had been silently lost from this file by a prior truncation bug and were recovered verbatim from git history (commit `b1309839`) as part of this same cleanup — see the archive's recovery note for detail. Nothing was deleted; see `HANDOFF-ARCHIVE.md` for the full verbatim entries.

---

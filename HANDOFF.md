# Psych101 — Current Handoff

**Last updated:** 2026-07-27
**Canonical repository:** `C:\GitHub\psych101`

This file records current state only. See `GPT_project_log.md` for completed-work history, `PROJECT_BACKLOG.md` for durable deferred work, and `AGENTS.md` for the chapter voice-conversion workflow and all conventions.

## Current state

**Chapter 9 has completed the spine-seed protocol (2026-07-27): Decision 1 approved, Decision 2 retired.** Record: `pipeline/ch09-spine-seeds.md`. The chapter was already strongly spine-connected around Build → Predict → Check → Revise, so the installed pass was small: the Language section now presents the communication-versus-thought dispute without resolving it; the existing *k*-word availability reveal now leads Section 2; two redundant explanations were trimmed; and the different-shortcuts seam now states what each shortcut discards. Body 6,306 → 6,254 words (**−52**). Generated Chapter 9 HTML is synchronized.

**The Chapter 9 diagnostic found and resolved a second stale `installed` status in `evidence-commitments.md`.** The Layer 3 note, ledger, provenance, and archived source described the bounded group-differences treatment as retained; the canonical chapter and instructor packet omitted it. Jon chose the current canonical scope and explicitly retired the thread from Chapter 9. `theoretical-spine.md` now says not to re-propose it, and the ledger retains the evidence boundary but marks it retired from Ch. 9. No group-differences prose was added, and the coupled learning-styles prune was not taken. By contrast, `semantic space` and `IQ as lossy compression` scored zero only as exact phrases; both ideas were already taught in plain language and were deliberately not seeded.

**Chapter 9 validation: 14/14 lint PASS, 0 warnings; coherence remains `fail=0 review=5`.** Bilalić, Fedorenko, Huth, and Mahowald are cited but missed by known narrative-citation parser limitations; Deary (2001) is genuinely uncited. Exact source/HTML checks confirmed the approved language paragraph and moved *k*-word lead, confirmed the retired group paragraph is absent, and confirmed the learning-styles callout remains. The checker also reports that the framing→language bridge is absent from objectives and summary and that the summary is 446 words, above its 400-word review target. Apparatus changes remain proposed rather than installed.

**Chapter 8 has been through the spine-seed protocol (2026-07-27), five decisions approved and installed.** Body 7,268 → 7,398 words (**+130**); 14/14 lint PASS, 0 warnings. Record: `pipeline/ch08-spine-seeds.md`. Coherence-checker output is **byte-identical to the pre-edit baseline** — this pass introduced no new failures and no new review items.

**The Ch. 8 finding is the most serious one any pass has produced: a committed claim was being violated verbatim while its ledger row read `installed`.** `evidence-commitments.md` → Ch. 8 → *Survival processing* says do not say memory was "tuned this way on purpose" or that forgetting is "the compression it evolved to do." Both strings were in `ch08-memory.md` L158, word for word, in consecutive sentences, in the one paragraph carrying the chapter's spine. The paragraph is rewritten: the pattern is asserted with full force, the design language is gone, the rival accounts (elaboration, distinctiveness, scenario richness) are named once in plain language as the row requires, and both strings now grep to 0. **The row's status cell now carries a dated note recording that it had been wrong**, plus the general lesson: *treat other `installed` statuses as unverified until checked.* Nobody has checked the rest — see next action 3.

**The structural finding: Ch. 8 was answering half its own spine question.** *"Why does memory preserve meaning better than detail?"* Section 3 answered the detail half completely. The meaning half — episodic compressing into semantic — appeared in exactly one place in the whole chapter, the Key Terms gloss on **Semantic memory** ("detached from the specific episode in which it was learned"). In the body, Section 2 drew episodic and semantic as sibling branches of a taxonomy tree, two table rows, and a damage dissociation, and never said one is made of the other. That is what `schema` 0, `gist` 0, and `verbatim` 0 were measuring. Decision 3 installs the pathway in ~105 words after the taxonomy table, worded as "most of what you know" rather than "everything" **specifically so the H.M. ledger row (limited new semantic learning) is not contradicted twenty lines later.** Separately, `priming` scored 6 with **zero body sentences** — every hit was figure alt text, caption, table cell, objective, or glossary; Decision 2 gives it 55 words of prose.

**Ch. 8's four evolutionary vocabulary zeros were investigated and deliberately not seeded.** `ecolog` 0, `evolution` 0, `life history` 0, `efficac` 0 — but L158 already carried the entire life-history argument in plain language, including the spine's own middle-school-morning-routine example. Ch. 6's precedent held again: **a vocabulary zero is not a conceptual zero**, and the diagnostic's job is to tell the two apart, not to trigger on the count.

**Chapter 7 has been through the spine-seed protocol (2026-07-27).** Chapters 1, 2, 3, 4, 5, 6, 7, 8, 11, 12, and 13 have now had the actual protocol run on them; every other chapter's spine question predates it. Ch. 7 was already strongly aligned around learning versus observable performance, so the pass installed two missing seams rather than rebuilding the chapter: extinction is now named as prediction-error learning, with dopamine reward prediction error explicitly bounded as one neural example; and Garcia and Koelling now carry the proximate/ultimate pair Chapter 1 promises will reappear here. Three sentence substitutions reduced body prose 4,172 → 4,159 words (**−13**). Record: `pipeline/ch07-spine-seeds.md`. Generated HTML is synchronized. Validation: 14/14 lint PASS with 0 warnings, no coherence failures, all 11 review questions retained, and `git diff --check` clean. The one coherence review is the documented apostrophe-slug false positive for the Little Albert anchor; the generated ID is correct.

**Chapter 6 has been through the spine-seed protocol (2026-07-27).** Ch. 6 was the last chapter in the book with no spine question at all, and now has one: *"What is your brain doing that it cannot do while you are awake?"* — installed in `theoretical-spine.md` §8 and at the head of `Where This Fits`, mirroring Ch. 8's placement. Body 8,393 → 8,455 words (**+62**), no prune required. Apparatus clean: 14/14 lint PASS with 0 warnings, and the coherence checker went from `review=1` to `CLEAN fail=0 review=0` — the one review item was the missing spine question, which this pass satisfied.

**Ch. 6 was not the chapter Ch. 5 was, and the diagnostic said so before any seeds were drafted.** All three functional layers the spine assigns it scored non-zero and are genuinely installed — `consolidat*` 11, `glymph*` 7, amygdala/prefrontal 7/8, `model` 11. The usual justification for a full pass did not apply, and the pass was correspondingly small: four decisions, two new sentences. Two zeros were investigated and deliberately not seeded. `episodic`/`semantic` at 0/0 is a **deliberate deferral, not a gap** — `ch08-memory.md` L19 states Ch. 6 "deliberately left thin" the consolidation thread so Ch. 8 could deliver it; do not re-propose it. Fatal familial insomnia was cut from the pre-audit draft during conversion and no current file records the deletion; flagged, not acted on.

**The finding was a false ranking, and fixing it surfaced a second defect.** `### What Sleep May Do` opened by presenting restoration, an evolutionary trade-off, and memory reorganization as "three of the strongest" accounts that "carry different amounts of support." Two of those are proximate and one is ultimate; ranking them on one evidence scale invites the student to read the evolutionary account as the weak one, which it is not. Separately, the glymphatic paragraph opened *"The restorative account gained a concrete mechanistic boost…"* — a callback across two intervening paragraphs to the account it exists to support. Both are repaired: the framing sentence is rewritten, the subsection is reordered into a proximate block then an ultimate block with a seam at the single switch, and the two developmental paragraphs moved to the end of `Sleep Architecture` where they belong.

**The proximate/ultimate labels were deliberately withheld, reversing the agent's own first recommendation.** The initial proposal was to install `proximate`/`ultimate` in bold in Ch. 6 and add "6" to Ch. 1's list, piloting the propagation. That was withdrawn: if Ch. 6 gets the labels and Chs. 7, 8, 11, 13 then do not, Ch. 1 L162 becomes wrong in *both* directions. The rewrite is jargon-free and **upgradeable** — dropping the two terms in is a one-sentence edit whenever the propagation runs.

**Ch. 1's forward promise is now partially repaired.** `ch01-history-approaches.md` L162 installs both terms in bold with Key Terms entries (L413, L425) and states *"you will see the pair again in Chapters 7, 8, 10, 11, and 13."* Actual delivery after the Ch. 7 pass: Ch. 7 **installed**, Ch. 8 **0**, Ch. 10 **1**, Ch. 11 **0**, Ch. 13 **0**. Two of five. Ch. 11 remains the highest-value gap because `theoretical-spine.md` L41 and L520 name it as the chapter where the distinction matters most. **Ch. 8's pass did not change its count, deliberately** — Decision 1 rewrote the chapter's evolutionary paragraph and could have dropped the labels in for free, but the Ch. 6 reasoning applies unchanged: installing them chapter-by-chapter as passes happen to land makes Ch. 1's promise wrong in a new direction each time. Ch. 8's rewritten paragraph, like Ch. 6's, is jargon-free and **upgradeable in one sentence** whenever the propagation runs as a single deliberate pass.

**Chapter 2's pre-line-edit hedge/wordiness audit (2026-07-27) is still awaiting one decision from the instructor.** Body prose went 4,844 → 3,981 words (−18%), body negation tokens 84 → 56 (−33%), no authorial first person left; fresh packet at `line-edit-packets/Chapter_2_Research_Methods_and_Statistics_Line_Edit_POST_AUDIT_2026-07-27.docx`, marked-up original untouched. **Open: is the no-first-person rule book-wide or Ch2-local?** If book-wide, `pipeline/voice-brief.md` §2 contradicts it (it lists "Occasional first-person disclosure" as a voice feature) and eight other chapters plus the prologue still contain first person, the prologue most heavily.

**Workflow for spine-seed passes is settled.** Chat iteration, not `.docx` packets, and serial — one chapter at a time. This has now held across Ch. 12 and Chs. 1–9; Chapter 9's decision gate and installation are complete. `spine-seed-protocol.md` Step 8 (delivery format) still describes the old packet flow and has not been rewritten to match. The `git show HEAD:<path>` baseline trick established on Ch. 5 is now the standard move — on Ch. 8 it proved that the only vocabulary leaving the chapter belonged to the four edited passages. **One correction to the recipe:** the version documented below omitted `tr -d '\r'` and produces hundreds of phantom differences on this repo's CRLF files, which looks like a catastrophic diff and is an artifact. Fixed in Repository notes.

**Ch. 7 and Ch. 8 ran in parallel on 2026-07-27 and both rewrote this file.** The Ch. 8 session found Ch. 7's rewrite mid-pass and merged into it rather than overwriting. If passes are run concurrently again, re-read `HANDOFF.md` immediately before writing it; a full-file `Write` from a stale read silently destroys the other session's record.

## Immediate next actions

1. **Chapter 9 apparatus retrofit — the only spine-pass work still open.** Drafted in `pipeline/ch09-spine-seeds.md` → Step 9 and proposed rather than installed because objectives are pedagogical commitments. The framing→language bridge is taught in the body but named in no objective and not joined in the summary; the summary is already 446 words, so repair by replacement/compression rather than addition. One review question should test the relation rather than another term distinction. Separately, Deary (2001) is genuinely uncited and needs a cite-or-move decision.
2. **Ch8 apparatus retrofit — the only thing Ch. 8 still owes.** Drafted in `pipeline/ch08-spine-seeds.md` → Step 9, proposed rather than installed, per the rule that objectives are pedagogical commitments. Learning Objective 3 reads *"Differentiate explicit from implicit memory, and episodic from semantic memory"* — pure term discrimination, and exactly the objective Decision 3 outdates; replacement drafted. Chapter Summary needs a one-clause fix for the same reason. Third, undecided: **should `schema` become a bolded Key Term?** Decision 3's prose deliberately avoids the word so the coherence checker does not demand an entry — the same move Ch. 5 made with *depersonalization*. Adding the term is a separate call from adding the idea.
3. **`Baddeley (2000)` is cited in Ch. 8 L63 and has no reference-list entry.** Real defect, pre-existing, surfaced by the verification pass; the list carries only Baddeley & Hitch (1974). Not fixed — the bibliographic record should be entered from the actual source, not from memory. This is what CLAUDE.md's citation check exists to catch, and it caught one.
4. **Audit the rest of `evidence-commitments.md` against the chapters.** Ch. 8's *Survival processing* row read `installed` while the prose said both forbidden things verbatim; Ch. 9's group-differences row is now confirmed as another stale installed status. A `grep` sweep of each row's forbidden strings against its own chapter is cheap and is now the highest-value book-wide check available.
5. **Ch. 7 figure numbering skips 7.8, and the likely missing asset is sitting on disk.** Captions now run 7.1–7.7 then **7.9** — eight figures, no 7.8, and no in-text reference to one either, so cross-references are self-consistent and nothing is broken. But `docs/images/ch07/fig_dopamine_uncertainty_curve.png` and `.svg` exist and are referenced by nothing, and a dopamine uncertainty curve is exactly what would sit between Tolman (7.7) and reward prediction error (7.9). Either that figure was dropped and the numbering never closed up, or it is still wanted. **Restore it or renumber 7.9 → 7.8 — a scope decision, not a mechanical fix.** Surfaced 2026-07-27 while verifying the Ch. 7 figure-expansion repair.
6. **Ch6 apparatus retrofit — the only thing Ch. 6 still owes.** Both items are drafted in `pipeline/ch06-spine-seeds.md` → Step 9 and were proposed rather than installed, per the rule that objectives are pedagogical commitments. Learning Objective 3 and Chapter Summary L182 both still describe the accounts as "differently supported" — the exact framing this pass removed from the body. Replacement wording for both is drafted. Third, undecided: whether the spine question should also appear in the Misconception Opener; L13 already closes on its declarative form, so making both interrogative may read as repetition.
7. **Ch5 apparatus retrofit — still open from the previous session.** Both items drafted in `pipeline/ch05-spine-seeds.md` → Step 9: a new learning objective naming the constructed-self thread (with LO 7 and 8 renumbering), and a Chapter Summary sentence covering it. Third, undecided: whether *depersonalization* becomes a Key Term. It is deliberately unbolded in the body so the checker does not demand an entry.
8. **`theoretical-spine.md` L386 is still inconsistent with the book** — it assigns the cognitive light cone to Ch. 5, which declined it. Re-point to Ch. 12. Unchanged from last session; Ch. 6's pass inserted lines below this point only, so the line number still holds.
9. **The proximate/ultimate propagation has started with Chapter 7.** Ch. 1's promise is now correct for Chs. 7 and 10 but still unfulfilled in Chs. 8, 11, and 13. Ch. 11 is the highest-value target. Ch. 6 is pre-wired for the labels but is not named in Ch. 1's promise.
10. **Ch4 — genuinely open, needs a decision before another pass is worth it:** the individual-differences taxonomy (sensory/experience-shaped/state-based) is still a real gap, but its planned flagship example (Müller-Lyer) was rejected on 2026-07-27 verification. Needs a different experience-shaped-priors example — a perceptual-learning or expertise case (echoing the radiologist example already in §3) was floated but not drafted.
11. **Ch11 — genuinely open, needs the instructor:** the "Social psychology as model-building organisms in contact" thread has no Learning Objective naming it. A scope decision, not a mechanical fix.
12. **Chapter 11 apparatus retrofit** — objectives/key terms partially updated in a prior session; confirm whether the full retrofit (review questions, summary) is still needed against `pipeline/ch11-spine-seeds.md`'s decisions.
13. **Ch12 — optional:** `cognitive light cone` isn't named in the Chapter Summary, though the idea is fully present in plain language. Slightly more load-bearing given Ch. 5 declined the thread and Ch. 12 formally owns it.
14. **No Chapter 6 section exists in `pipeline/evidence-commitments.md`.** Four bounded claims in the chapter would qualify — the glymphatic human-evidence boundary, the Yoo et al. (2007) correlational hedge, the Spiegel et al. (2004) sex/weight variability, and the Cohen et al. (2009) observed-not-assigned caveat. Whether they should be recorded is a decision, not a fix.
15. Keep the Chapter 6 and Chapter 7 image lanes paused unless reopened.
16. `pipeline/check_chapter_coherence.py --all` currently crashes on Chapter 9 with a `UnicodeEncodeError` (Windows console `cp1252` can't encode `ć`) — pre-existing, not fixed. Run it per-chapter-path until someone fixes the encoding or redirects output to a UTF-8 sink.
17. **Ch2's remaining coherence review items are checker noise, not chapter defects** — two "and colleagues"-phrased narrative citations (Lieberman 1999, McGregor 1998) and one multi-year combined citation (Hróbjartsson 2010). Verified genuinely cited; not acted on.

## Files changed by the current Chapter 9 spine-seed pass, uncommitted

- `source/chapters/09-thinking-language-intelligence.md` — approved Decision 1 plus the recorded Section 2 lead inversion and trims
- `docs/chapters/09-thinking-language-intelligence.html` — regenerated from canonical Markdown
- `pipeline/ch09-spine-seeds.md` — diagnostic, decisions, installed trade, and verification
- `pipeline/theoretical-spine.md` — group-differences thread explicitly retired from Chapter 9
- `pipeline/evidence-commitments.md` — corresponding status corrected from installed to retired
- `source/chapters/_provenance/ch09-thinking-language-intelligence.md` — decision reconciliation record
- `GPT_project_log.md` — newest-first completion entry
- `HANDOFF.md` — current completion state and immediate next action

No Chapter 9 figure, objective, summary, reference, review question, lab, commit, or remote branch changed.

## Builder — all 13 chapters registered

`pipeline/build_chapter_html.mjs` registers Chapters **1–13**. The prologue is built separately and is not part of this registry. A lint PASS is a reliable signal for every numbered chapter; 14/14 currently PASS with 0 warnings.

**The builder rewrites all thirteen HTML files on every run**, so after a single-chapter pass the other twelve may surface as modified even when only one chapter's content changed. Stage exact paths.

## Repository notes

- **`git status` is safe in a native shell (Claude Code).** The lock-file hazard below is specific to the Cowork sandbox mount.
- **Cowork sandbox mount:** do not run `git add`, `git commit`, or `git update-ref` there — it can create `.git/index.lock`/`.git/HEAD.lock` but can't unlink them, which blocks every subsequent git command until deleted by hand. Safe read-only commands under that mount are `git log`, `git diff`, `git show`, `git rev-list`. **Use `git show HEAD:<path> > /tmp/...` to take a pre-edit baseline** rather than `git stash`; it is read-only and avoids the lock entirely (established Ch. 5, reused Ch. 6).
- **Verify a reorder or a prune by diffing sorted word content against the baseline.** Correct recipe, fixed on Ch. 8: `diff <(tr -d '\r' < base | tr -s ' \n' '\n' | sort) <(tr -d '\r' < new | tr -s ' \n' '\n' | sort)`. **The version previously documented here omitted `tr -d '\r'`, and on this repo's CRLF files it produces hundreds of phantom differences** — it looks like a catastrophic diff and is entirely an artifact. With the fix, Ch. 8's removed-word list contained only vocabulary from the four edited passages, proving nothing else was dropped. Cheap, and stronger than eyeballing the diff.
- **Baseline the coherence checker before editing, not just after.** Ch. 8's post-edit output was `fail=2 review=2`, which reads alarming until compared against the pre-edit baseline, which was byte-identical. Running the checker on the `git show` baseline copy costs one command and converts "did I break this?" into a fact.
- **`check_chapter_coherence.py` has several documented false-positive classes** (see `PROJECT_BACKLOG.md` → Editorial review for full detail): its citation regex can't parse year-first narrative citations, three-author comma-list narrative citations, "Author and colleagues (year)" phrasing, or multi-year combined citations (only the first year is captured); its institutional-author keying picks a different word depending on citation vs. reference-list direction, so a correct citation can read as missing in both directions at once — **this is what produces Ch. 5's standing `Gelder (2008)` failure, where the reference list correctly reads `de Gelder, B.`**; its reference-parenthetical regex requires a bare year and rejects a correctly-formatted date like `(2024, September 4)`; its Key Terms check treats a slash-joined term ("X / Y") as one literal string, which will essentially never match; and its Connections-anchor slugifier doesn't match the JS builder's apostrophe-handling for `#### Classic Study` callout divs. **Add one more class, confirmed on Ch. 8: the Key Terms body-presence check is a literal singular match, so a term glossed as "Flashbulb memory" reads as absent from a body that says "Flashbulb memories" ten times.** Always verify a suggested fix against `lint_chapters.py` or by hand against the References list before applying it.
- **The checker does read the spine question** out of `theoretical-spine.md` §8 and check for its presence in the chapter body. Ch. 6's pre-edit baseline flagged it as absent; installing it cleared the review. Useful signal, not noise.
- **`pipeline/evidence-commitments.md` has one OPEN row** — Ch. 10's life-history mosaic point (short interbirth intervals and high reproductive output alongside slow development) was requested and is not in the source. It also has **no Chapter 6 section at all** (see next action 12). **And as of 2026-07-27 its `installed` statuses are known to be unreliable:** Ch. 8's *Survival processing* row read `installed` while both of its forbidden strings sat in the chapter verbatim. See next action 3.
- **Three files in `docs/images/ch03/` are not images.** Despite `.png` extensions they are UTF-8 text notes, producing a recurring ~10-byte CRLF phantom diff. They reappear as modified until renamed to `.txt` or the one-time renormalization runs.
- **A revised packet always goes to a new filename** if packets are used again — a packet was once rebuilt in place over the instructor's entered answers and they were unrecoverable.
- Deferred and needing a decision, in `PROJECT_BACKLOG.md`: removing ~20 MB of unreferenced images from `docs/`; the one-time `git add --renormalize .`; the twelve unreviewed coherence-check failures; and the book-wide finding that banned throat-clearing rose in 11 of 11 audited chapters.

## Chapter 13 — open, none of it blocking

1. **The five maintenance-lens disclaimers.** The proximate/ultimate install does not by itself remove the hedges the 2026-07-15 repair pass added. Separate cleanup, still wanted.
2. **Register.** 1.3 second-person uses per 1,000 body words — the lowest in the book by a wide margin. About half of body paragraphs still end on a negation.
3. **Does the spine question move into the opener?** *"When does a protective model become costly?"* appears only in figure captions and alt text. Ch. 12 precedent says move it; Ch. 6 has now followed that precedent too.
4. **Section 3 is 435 words**, roughly a third of its neighbours. Its position is correct and worth keeping; whether that justifies the length is a separate question.
5. **Grupe & Nitschke (2013)** remains open for Ch. 13 in `evidence-commitments.md`.
6. **Opener prevalence figure** says "roughly one in five U.S. adults." NIMH's current figure is 23.1%. The existing hedge covers it; the phrasing is on the low side of the source.
7. **Five reference-list entries are never cited in text** — Barth et al. (2013), Beck et al. (1979), Craske et al. (2014), Linehan (1993), Rogers (1957). Linehan and Rogers are discussed by name without a parenthetical; the other three are unused. Cite them or move them to Further Reading.

## Important files

- **Spine-seed protocol:** `pipeline/spine-seed-protocol.md` — delivery format (Step 8) is stale; the instructor wants chat iteration, not `.docx` packets, and this has now held across seven passes.
- **Chapter 1 seeds and settled decisions:** `pipeline/ch01-spine-seeds.md`
- **Chapter 2 seeds and settled decisions:** `pipeline/ch02-spine-seeds.md`
- **Chapter 3 seeds and settled decisions:** `pipeline/ch03-spine-seeds.md`
- **Chapter 4 seeds and settled decisions (including the Müller-Lyer re-verification):** `pipeline/ch04-spine-seeds.md`
- **Chapter 5 seeds, six settled decisions, and the rubber-hand-illusion finding:** `pipeline/ch05-spine-seeds.md`
- **Chapter 6 seeds, four settled decisions, and the Ch. 1 L162 propagation measurement:** `pipeline/ch06-spine-seeds.md`
- **Chapter 7 seeds, two settled decisions, and the first proximate/ultimate propagation repair:** `pipeline/ch07-spine-seeds.md`
- **Chapter 9 seeds and settled decisions:** `pipeline/ch09-spine-seeds.md`
- **Chapter 11 seeds and decision record:** `pipeline/ch11-spine-seeds.md`; conversion residue: `pipeline/audits/ch11-conversion-residue-2026-07-25.md`
- **Chapter 12 seeds and all nine settled decisions:** `pipeline/ch12-spine-seeds.md`
- **Chapter 13 seeds, settled decisions, and verification run:** `pipeline/ch13-spine-seeds.md`
- **Evidence commitments (claim-level constraints, book-wide):** `pipeline/evidence-commitments.md`
- Chapter voice-conversion workflow: `AGENTS.md` → `## Style`
- Theoretical spine (Layer 3 + drafting rules): `pipeline/theoretical-spine.md`; elaborations in `pipeline/theoretical-foundations.md`, `GPT_spine.md`, `pipeline/concepts-semanticization-log.md`
- Validators: `pipeline/lint_chapters.py` (structure, on generated HTML), `pipeline/check_chapter_coherence.py` (apparatus, on Markdown source — takes explicit paths or `--all`; `--all` currently crashes on Ch9's Unicode; has known false-positive classes, see Repository notes)
- Shared builder: `pipeline/build_chapter_html.mjs` (Chapters 1–13 registered); line-edit packet builder: `pipeline/build_line_edit_docx.py`
- Durable deferred work: `PROJECT_BACKLOG.md`
